访问者模式是行为型设计模式，他的应用场景是当有一组相同父类的元素执行各自的公共方法，但是根据需求变化执行逻辑有很多分支时，
可以把执行逻辑转移出去，交给访问者的方法来做，可以定义多个访问者，提前封装好执行逻辑，从而使得方法的执行可以根据需求，调整
访问者的选择，他的缺陷是，当需要新增一个子类时，访问者需要同时添加对应的执行方法，这导致访问者不稳定，并且把类自身的执行
逻辑交给其他类来定义本身就破坏了类型的封装职责单一原则，这里的启发是，类的定义是根据需求来的，不一定遵循类的定义规范。
访问者的方法是提前写死的，是把同一批接口实现类的执行逻辑整合到另一个类中。