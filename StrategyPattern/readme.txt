策略模式是行为型设计模式，他更关注根据条件不同来执行不同的策略，也就是执行不同的方法逻辑，他是一种简单的设计模式，
目的只是想把平时简单的if else 这样面向过程的代码并且执行方式差不多的过程，通过类封装起来，也能提供额外的执行逻辑，
并交给一个统一的上下文类执行，便于代码的可阅读性，同时顺应需求的变化，可以新增不同的策略，解决在实际开发过程中，
因需求变动而修改执行过程中的源代码的困扰。但不得不说，这种设计模式是依赖细节的，可以通过工厂+配置文件+反射解决问题。