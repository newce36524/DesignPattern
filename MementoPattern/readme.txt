备忘录模式是行为型模式的一种，在不破坏类型封装的前提下能够保存类型运行时内部的状态，提供在需要时可以获取保存的状态，
他的原理是让一个管理者对象通过内部维护一个存储区(可以是缓存，可以是数据库，可以是文本文件。。。)来保存备忘对象，
这个备忘对象专门用于保存指定类型的属性值，以在需要时可以通过管理者对象获取到保存的属性值并覆盖指定对象的属性值。
他的缺点是会让项目变得笨重，需要把无关类职责的方法定义在类中，破坏了职责单一原则。区分类型封装和职责单一。

